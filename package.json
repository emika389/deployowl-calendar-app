{
  "name": "deployowl-calendar-app",
  "version": "1.0.2",
  "description": "DeployOwl Calendar App — a serverless calendar app running on DeployWeb edge CDN",
  "type": "module",
  "scripts": {
    "validate": "echo 'Validating...'",
    "build": "mkdir -p dist && cp -r api dist/",
    "test": "node --check api/index.js",
    "deploy": "bash deploy.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
