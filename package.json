{
  "name": "deployowl-calendar-app",
  "version": "1.0.2",
  "description": "Gift Calendar — a serverless calendar app running on DeployWeb edge CDN",
  "type": "module",
  "scripts": {
    "validate": "echo 'Validating...'",
    "build": "mkdir -p dist/api && cp -r api/* dist/api/",
    "deploy": "bash deploy.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
